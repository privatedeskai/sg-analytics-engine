{
  "name": "sg-analytics-engine-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail --format pretty"
  },
  "devDependencies": {
    "wrangler": "3.99.0"
  }
}
